{
  "name": "image-differ",
  "version": "0.1.0",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "clean:lib": "rimraf lib",
    "clean:example": "rimraf example/dist",
    "build:lib": "tsc -p .",
    "build:example": "tsc -p example/",
    "example": "cd example/ && node dist/simple.js",
    "build": "run-s clean:* build:*"
  },
  "devDependencies": {
    "@types/pixelmatch": "^4.0.0",
    "@types/pngjs": "^3.3.2",
    "@types/puppeteer": "^1.12.3",
    "eslint": "^5.16.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^1.16.4",
    "rimraf": "^2.6.3",
    "typescript": "^3.4.3"
  },
  "dependencies": {
    "pixelmatch": "^4.0.2",
    "pngjs": "^3.4.0",
    "puppeteer": "^1.14.0"
  }
}
